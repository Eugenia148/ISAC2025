# ==========================================
# env.example
# Example environment variables file for StatsBomb API credentials
# ==========================================
# 
# Copy this file to .env and fill in your actual credentials
# 
# StatsBomb API Credentials (Optional - for premium data access)
# Choose ONE naming convention and stick with it:

# Option 1: Short names (recommended)
SB_USERNAME=your_statsbomb_username
SB_PASSWORD=your_statsbomb_password

# Option 2: Full names (alternative)
# STATSBOMB_USER=your_statsbomb_username
# STATSBOMB_PASS=your_statsbomb_password

# Option 3: Alternative full names (also works)
# STATSBOMB_USERNAME=your_statsbomb_username
# STATSBOMB_PASSWORD=your_statsbomb_password

# Note: If no credentials are provided, the system will use open data access only.
# Most Liga MX data is available without authentication, but premium datasets may require credentials.
