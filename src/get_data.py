# ==========================================# get_data.py# Connection to the API and data download# ==========================================## Install packages#pip install python-dotenv#pip install os#pip install pandas#pip install numpy## Packagesimport osimport pandas as pdimport numpy as npimport matplotlib.pyplot as pltimport matplotlib.patheffects as path_effectsfrom statsbombpy import sbfrom mplsoccer.pitch import VerticalPitchfrom matplotlib.colors import LinearSegmentedColormapfrom dotenv import load_dotenv## Upload credentials from .envload_dotenv(dotenv_path="../.env")EMAIL = os.getenv("STATSBOMB_USER")PASSWORD = os.getenv("STATSBOMB_PASS")## ParametersDATA_DIR = "../data"       # Carpeta donde se guardarán los datosTEAM = "Club América" # Poner el nombre del equipoCOMP_ID = 108                # Competición (ejemplo: Premier League)SEASON_ID = 506            # Temporada (ejemplo: 2023/2024)